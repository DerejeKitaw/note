```js
return (
    <div>
    {season === 'winter'? 'It is chilly': 'Summer time'}
    </div>
```
to

```
const text = season === 'winter' ? 'It is chilly' : 'Summer time';
  return (
    <div>
      {text}
    </div>
  );
  ```
  
  

